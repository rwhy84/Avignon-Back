{% extends 'base-admin.html.twig' %}

{% block dashboard %}


                        <div class="row justify-content-center">
                        <div class="col-lg-10 col-xl-10">
                        <h2 class="title-1 m-b-25">Tous les evenements:</h2>
                        <div class="table-responsive table--no-card m-b-40">
<table class="table table-borderless table-striped table-earning">
<thead>
<tr>
<th>Nom</th>
{# <th class="text-right">Etablissement</th> #}
<th class="text-right">Date de début</th>
<th class="text-right">Date de fin</th>
<th class="text-right">Modifier</th>
<th class="text-right">Supprimer</th>
</tr>
</thead>
<tbody>


{% for event in event %}
<tr>
<td style="font-weight: bold">{{ event.name |slice(0, 20) }}</td>
{# <td>{{event.etablissement}}</td> #}

{# <td class="text-right">{{event.etablissement}}</td> #}
<td class="text-right">{{event.startEvent |date("d/m/Y")}}</td>
<td class="text-right">{{event.endEvent |date("d/m/Y")}}</td>
<td class="text-right"><a class="btn btn-warning" href="{{ path('event_edit',{'id': event.id}) }}">Editer</a></button></td>
<td class="text-right"><a class="btn btn-danger" href="{{ path('event_delete',{'id': event.id}) }}">Supprimer</a></button></td>
</tr>
    
{% endfor %}

</tbody>
</table>

<div class="navigation">
    {{knp_pagination_render(event)}}
</div>
</div>
</div>


    
{% endblock %}