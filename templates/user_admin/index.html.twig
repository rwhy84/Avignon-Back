{% extends 'base-admin.html.twig' %}

{% block dashboard %}



                        <div class="row justify-content-center">
                        <div class="col-lg-10 col-xl-10">
                        <h2 class="title-1 m-b-25">Tous les utilisateurs:</h2>
                        <div class="table-responsive table--no-card m-b-40">
<table class="table table-borderless table-striped table-earning">
<thead>
<tr>
<th>Nom</th>
{# <th class="text-right">Etablissement</th> #}
<th class="text-right">Modifier</th>
<th class="text-right">Supprimer</th>
</tr>
</thead>
<tbody>


{% for user in user %}
<tr>
<td style="font-weight: bold">{{ user.email  }}</td>
{# <td>{{event.etablissement}}</td> #}

{# <td class="text-right">{{event.etablissement}}</td> #}
<td class="text-right"><a class="btn btn-warning" href="{{ path('user_edit',{'id': user.id}) }}">Editer</a></td>
<td class="text-right"><a class="btn btn-danger" href="{{ path('user_delete',{'id': user.id}) }}">Supprimer</a></td>
</tr>
    
{% endfor %}

</tbody>
</table>

<div class="navigation">
    {{knp_pagination_render(user)}}
</div>
</div>
</div>

    
{% endblock %}